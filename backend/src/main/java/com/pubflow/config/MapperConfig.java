package com.pubflow.config;

/**
 * Placeholder class (no Spring beans) to avoid forcing MapStruct instantiation at startup.
 * MapStruct mappers are discovered via component scanning (@Mapper componentModel="spring").
 */
class MapperConfig {
}
